name: "6. Inputs"
run-name: "6. Inputs"
on: [push]

jobs:
  call-reusable-workflow:
    uses: ./.github/workflows/6.input-part.yml
    with:
      your_name: "itenium"
    secrets:
      PASS: "Sssh!"
